# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Local environment overrides
.env

# State & logs (local, not committed)
scripts/state.json
scripts/*.log
scripts/session-flush-*
scripts/flush-test-marker.txt
scripts/locks/
scripts/.last-flush-spawn
scripts/.last-tool-capture
scripts/.last-wiki-reminder

# uv
uv.lock

# Obsidian
.obsidian/

# Personal content (not shared in repo)
wiki/
daily/
raw/
reports/
log.md
index.md
# docs/ tracked since 2026-04-14: contains Codex handoff plans/reports
# referenced from PR descriptions. New files MUST use ${USER} placeholder
# (no real usernames). See feedback_codex_task_handoff_workflow memory.

# Working configs with personal paths (use .example versions)
skills/wiki-save/SKILL.md
codex-hooks.example.json
scripts/project_aliases.local.json

# OS
.DS_Store
Thumbs.db

# Claude local workspace
.claude/
